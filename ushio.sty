% This file is auto-generated by build.py. Do not edit directly.

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{ushio}

\RequirePackage{amsmath}
\RequirePackage{amssymb}
\RequirePackage{mathtools}
\RequirePackage{mathrsfs}
\RequirePackage{bm}
\RequirePackage{bbm}

% Theorems
\RequirePackage{amsthm}
\RequirePackage{thmtools}
\RequirePackage{thm-restate}

\RequirePackage{graphicx}
\RequirePackage{wrapfig}

\RequirePackage{booktabs}
\RequirePackage{multirow}
\RequirePackage{tabularx}

\RequirePackage{hyperref}

\RequirePackage{caption}
\RequirePackage{subcaption}
\RequirePackage{enumitem}
% titlesec, geometry?

\RequirePackage[euler]{textgreek}

% https://jp.mirrors.cicku.me/ctan/macros/latex/contrib/cleveref/cleveref.pdf
% "Basically, cleveref must be loaded last."
\RequirePackage{cleveref}

% Theorem environments have to be declared after cleveref is loaded.
% Otherwise, `\cref' cannot find how to refer to them.

\declaretheorem[name=Theorem,numberwithin=section]{theorem}
\declaretheorem[name=Proposition,sibling=theorem]{proposition}
\declaretheorem[name=Lemma,sibling=theorem]{lemma}
\declaretheorem[name=Corollary,sibling=theorem]{corollary}
\declaretheorem[style=definition,name=Definition,sibling=theorem]{definition}
\declaretheorem[style=definition,name=Assumption,sibling=theorem]{assumption}
\declaretheorem[style=definition,name=Example,sibling=theorem]{example}
\declaretheorem[style=definition,name=Example,numbered=no]{example*}
\declaretheorem[style=remark,name=Remark,sibling=theorem]{remark}
\declaretheorem[style=remark,name=Remark,numbered=no]{remark*}

% Greek
\renewcommand{\epsilon}{\varepsilon}

% sets
\newcommand{\R}{\mathbb{R}} % real numbers
\newcommand{\C}{\mathbb{C}} % complex numbers
\newcommand{\Q}{\mathbb{Q}} % rational numbers
\newcommand{\N}{\mathbb{N}} % natural numbers
\newcommand{\Z}{\mathbb{Z}} % integers

% brackets etc
\newcommand{\set}[1]{\left\{ #1 \right\}}
\newcommand{\abs}[1]{\left\lvert #1 \right\rvert} % absolute value
\newcommand{\norm}[2][]{\left\lVert #2 \right\rVert_{#1}} % norm
\newcommand{\supnorm}[1]{\norm[\infty]{#1}} % sup norm
\newcommand{\enorm}[1]{\norm[2]{#1}} % Euclidean norm
\newcommand{\opnorm}[1]{\norm[\mathrm{op}]{#1}} % operator norm
\newcommand{\fnorm}[1]{\norm[\mathrm{F}]{#1}} % Frobenius norm
\newcommand{\psinorm}[2][]{\norm[\psi_{#1}]{#2}} % p-th Orlicz norm
\newcommand{\sgnorm}[1]{\psinorm[2]{#1}} % sub-gaussian norm
\newcommand{\senorm}[1]{\psinorm[1]{#1}} % sub-exponential norm
\newcommand{\lipnorm}[1]{\norm[\mathrm{Lip}]{#1}} % Lipschitz norm

% misc
\newcommand{\sign}{\operatorname{sign}}
\renewcommand{\hat}{\widehat}

\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator*{\argmin}{arg\,min}

\DeclareMathOperator{\Tr}{Tr} % trace
\DeclareMathOperator{\tr}{tr} % trace
\DeclareMathOperator{\Ker}{Ker} % kernel
\DeclareMathOperator{\Img}{Im} % image
\DeclareMathOperator{\rank}{rank} % rank
